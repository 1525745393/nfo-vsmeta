# demo_metadata_enhancer

示例元数据增强插件

- **作者**: Demo Author
- **版本**: 1.0.0
- **类型**: enhancer
- **优先级**: 50

## 安装

将此目录复制到转换器的 `plugins/` 目录下，重启转换器或使用热重载功能。

## 配置

编辑 `config.json` 或在 WebUI 插件管理页面中配置。

## 开发

修改 `plugin.py` 后，如果启用了热重载，插件会自动重新加载。
